# Data Layout

- `raw/`: source PDFs and Word files
- `interim/`: extracted or chunked intermediate files if needed
- `processed/faiss_index/`: saved vector store

Do not manually edit processed index files.
